spring.application.name=fintech
# ===================================================================
# CONFIGURA��O DO BANCO DE DADOS ORACLE (FIAP)
# ===================================================================
# Substitua pela URL, usu�rio e senha fornecidos pela FIAP
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=RM563970
spring.datasource.password=230997
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver

# ===================================================================
# CONFIGURA��O DO JPA / HIBERNATE (O "C�rebro" do Spring Data JPA)
# ===================================================================
# Define o "dialeto" SQL que o Hibernate vai usar para falar com o Oracle
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# Mostra no console o SQL que o JPA est� executando. �timo para debug!
spring.jpa.show-sql=true

# Formata o SQL mostrado no console para ficar mais leg�vel
spring.jpa.properties.hibernate.format_sql=true

# ===================================================================
# ATEN��O AQUI: Como o JPA gerencia suas tabelas (Item 6 do trabalho)
# ===================================================================
# 'validate': Verifica se as tabelas no Oracle batem com suas @Entity. Se n�o bater, d� erro.
# 'update': Tenta alterar as tabelas no Oracle para bater com suas @Entity. (�til, mas arriscado)
# 'create': Apaga TUDO e cria as tabelas do zero toda vez que a aplica��o sobe. (Use para testes)
# 'none': N�o faz nada.
#
# Para o seu trabalho, o ideal � usar 'validate' e voc� mesmo criar as tabelas (Passo 6).
# Por enquanto, podemos usar 'update' para acelerar o desenvolvimento.
spring.jpa.hibernate.ddl-auto=update